<h1>Listing distributors</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Website</th>
  </tr>

<% @distributors.each do |distributor| %>
  <tr>
    <td><%=h distributor.name %></td>
    <td><%=h distributor.website %></td>
    <td><%= link_to 'Show', distributor %></td>
    <td><%= link_to 'Edit', edit_distributor_path(distributor) %></td>
    <td><%= link_to 'Destroy', distributor, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New distributor', new_distributor_path %>